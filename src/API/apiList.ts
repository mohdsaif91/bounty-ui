const v1 = "/api/v1";

const developerV1 = `${v1}/developer`;
const contractV1 = `${v1}/contract`;

export const apiList = {
  addDeveloper: `${developerV1}/add`,
  getDeveloper: `${developerV1}`,
  uploadContract: `${contractV1}/upload`,
};
